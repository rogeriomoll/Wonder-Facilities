# Frontend Next.js - Condomínio Marketplace

## Rodar localmente
```bash
cd frontend
npm install
npm run dev
```
Deploy no Vercel: Vercel detecta automaticamente Next.js e faz build com `npm run build`.
